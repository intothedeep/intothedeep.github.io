/** @type {import('postcss-load-config').Config} */
const config = {
    // presets: [
    //     ['@babel/preset-env', { targets: 'defaults', useBuiltIns: false }],
    // ],
    plugins: {
        '@tailwindcss/postcss': {},
    },
};

export default config;
