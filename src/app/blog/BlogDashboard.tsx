import clsx from 'clsx';
import React from 'react';

type Props = {};

export const BlogDashboard = async (props: Props) => {
    return (
        <main className={clsx('')}>
            <h1>Blog Dashboard</h1>
        </main>
    );
};

export default BlogDashboard;
